{
  "name": "happy-tails-server",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=server\"",
    "build": "npm run build --workspace=frontend",
    "start": "NODE_ENV=production node server/server.js"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^17.2.4"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
